<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="JualIn.App.Mobile.Presentation.Modules.Auth.Views.SignUp_Step1_Page"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:converters="clr-namespace:JualIn.App.Mobile.Presentation.UI.Controls.Converters"
    xmlns:horus="clr-namespace:HorusStudio.Maui.MaterialDesignControls;assembly=HorusStudio.Maui.MaterialDesignControls"
    xmlns:icons="clr-namespace:UraniumUI.Icons.MaterialSymbols;assembly=UraniumUI.Icons.MaterialSymbols"
    xmlns:inputkit="clr-namespace:InputKit.Shared.Controls;assembly=InputKit.Maui"
    xmlns:pages="clr-namespace:JualIn.App.Mobile.Presentation.Modules.Auth.Views"
    xmlns:strings="clr-namespace:JualIn.App.Mobile.Presentation.Resources.Strings"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    xmlns:uranium="http://schemas.enisn-projects.io/dotnet/maui/uraniumui/material"
    xmlns:validations="clr-namespace:InputKit.Shared.Validations;assembly=InputKit.Maui"
    xmlns:vm="clr-namespace:JualIn.App.Mobile.Presentation.Modules.Auth.ViewModels"
    x:Name="root"
    Title="SignUp"
    x:DataType="{x:Type vm:SignUp_Step1_ViewModel}">
    <ContentPage.Behaviors>
        <toolkit:EventToCommandBehavior
            BindingContext="{Binding Path=BindingContext, Source={x:Reference root}, x:DataType={x:Type pages:SignUp_Step1_Page}}"
            Command="{Binding AppearingCommand}"
            EventName="Appearing" />
    </ContentPage.Behaviors>

    <ScrollView>
        <VerticalStackLayout
            Margin="{StaticResource AppLayoutPadding}"
            HorizontalOptions="Fill"
            Spacing="{StaticResource CommonSpaceBetweenSmall}">
            <Image
                Margin="{StaticResource CommonSpaceLargeTop}"
                Aspect="AspectFit"
                HeightRequest="72"
                Source="img_logo.png"
                WidthRequest="72" />

            <horus:MaterialLabel
                HorizontalOptions="Center"
                HorizontalTextAlignment="Center"
                Text="{Static strings:AppStrings.SignUpPage_Lbl_Title}"
                Type="TitleLarge" />
            <horus:MaterialLabel
                HorizontalOptions="Center"
                HorizontalTextAlignment="Center"
                Text="{Static strings:AppStrings.SignUpPage_Lbl_SubTitle}" />

            <inputkit:FormView
                Margin="{StaticResource CommonSpaceMediumY}"
                Spacing="{StaticResource CommonSpaceBetweenSmall}"
                SubmitCommand="{Binding RegisterCommand}">
                <uranium:TextField
                    Title="{Static strings:AppStrings.SignUpPage_Lbl_FormTitle_Password}"
                    AllowClear="True"
                    ClearButtonVisibility="WhileEditing"
                    IsPassword="True"
                    Text="{Binding Password}">
                    <uranium:TextField.Attachments>
                        <uranium:TextFieldPasswordShowHideAttachment />
                    </uranium:TextField.Attachments>
                    <uranium:TextField.Validations>
                        <validations:RequiredValidation />
                    </uranium:TextField.Validations>
                </uranium:TextField>
                <uranium:TextField
                    Title="{Static strings:AppStrings.SignUpPage_Lbl_FormTitle_ConfirmPassword}"
                    AllowClear="True"
                    ClearButtonVisibility="WhileEditing"
                    IsPassword="True"
                    Text="{Binding ConfirmPassword}">
                    <uranium:TextField.Attachments>
                        <uranium:TextFieldPasswordShowHideAttachment />
                    </uranium:TextField.Attachments>
                    <uranium:TextField.Validations>
                        <validations:RequiredValidation />
                        <validations:RegexValidation Message="{Static strings:AppStrings.SignUpPage_Lbl_PasswordNotMatch}" Pattern="{Binding Password}" />
                    </uranium:TextField.Validations>
                </uranium:TextField>
                <uranium:TextField
                    Title="{Static strings:AppStrings.SignUpPage_Lbl_FormTitle_FirstName}"
                    AllowClear="True"
                    ClearButtonVisibility="WhileEditing"
                    Text="{Binding FirstName}">
                    <uranium:TextField.Validations>
                        <validations:RequiredValidation />
                    </uranium:TextField.Validations>
                </uranium:TextField>
                <uranium:TextField
                    Title="{Static strings:AppStrings.SignUpPage_Lbl_FormTitle_LastName}"
                    AllowClear="True"
                    ClearButtonVisibility="WhileEditing"
                    Text="{Binding LastName}" />
                <uranium:TextField
                    Title="{Static strings:AppStrings.SignUpPage_Lbl_FormTitle_BusinessName}"
                    AllowClear="True"
                    ClearButtonVisibility="WhileEditing"
                    Text="{Binding BusinessName}" />
                <uranium:PickerField
                    Title="{Static strings:AppStrings.SignUpPage_Lbl_FormTitle_BusinessCategory}"
                    AllowClear="True"
                    ItemsSource="{Binding BusinessCategorySuggestions}"
                    SelectedItem="{Binding BusinessCategory}" />

                <Button
                    inputkit:FormView.IsSubmitButton="True"
                    StyleClass="FilledButton"
                    Text="{Static strings:AppStrings.SignUpPage_Btn_Register}" />
            </inputkit:FormView>

            <Grid ColumnDefinitions="*,auto,*" ColumnSpacing="{StaticResource CommonSpaceBetweenSmall}">
                <horus:MaterialDivider Grid.Column="0" />
                <Label
                    Grid.Column="1"
                    FontSize="{StaticResource FontSizeShrink1}"
                    HorizontalOptions="Center"
                    Text="{Static strings:AppStrings.SignUpPage_Lbl_SocialSignUpDivider}"
                    VerticalTextAlignment="Center" />
                <horus:MaterialDivider Grid.Column="2" />
            </Grid>

            <VerticalStackLayout
                Margin="{StaticResource CommonSpaceSmallY}"
                HorizontalOptions="Fill"
                Spacing="{StaticResource CommonSpaceBetweenSmall}">
                <uranium:ButtonView
                    BackgroundColor="White"
                    HeightRequest="{StaticResource MinTouchTargetSize}"
                    Stroke="{AppThemeBinding Light={StaticResource Primary},
                                             Dark={StaticResource DarkPrimary}}"
                    StrokeShape="{RoundRectangle CornerRadius=20}"
                    TappedCommand="{Binding SignUpWithGoogleCommand}">
                    <HorizontalStackLayout HorizontalOptions="Center" Spacing="{StaticResource CommonSpaceBetweenSmall}">
                        <Image HeightRequest="{StaticResource FontSizeBase}" Source="ic_google" />
                        <horus:MaterialLabel Text="{Static strings:AppStrings.SignUpPage_Btn_SignUpWithGoogle}" VerticalTextAlignment="Center" />
                    </HorizontalStackLayout>
                </uranium:ButtonView>
                <uranium:ButtonView
                    BackgroundColor="#3B5998"
                    HeightRequest="{StaticResource MinTouchTargetSize}"
                    StrokeShape="{RoundRectangle CornerRadius=20}"
                    TappedCommand="{Binding SignUpWithFacebookCommand}">
                    <HorizontalStackLayout HorizontalOptions="Center" Spacing="{StaticResource CommonSpaceBetweenSmall}">
                        <Image HeightRequest="{StaticResource FontSizeBase}" Source="ic_facebook" />
                        <horus:MaterialLabel
                            Text="{Static strings:AppStrings.SignUpPage_Btn_SignUpWithFacebook}"
                            TextColor="{AppThemeBinding Light={StaticResource OnPrimary},
                                                        Dark={StaticResource DarkOnPrimary}}"
                            VerticalTextAlignment="Center" />
                    </HorizontalStackLayout>
                </uranium:ButtonView>
            </VerticalStackLayout>

            <HorizontalStackLayout HorizontalOptions="Center" Spacing="{StaticResource CommonSpaceBetweenSmall}">
                <horus:MaterialLabel Text="{Static strings:AppStrings.SignUpPage_Lbl_AlreadyHaveAccount}" VerticalTextAlignment="Center" />
                <horus:MaterialLabel
                    Text="{Static strings:AppStrings.SignUpPage_Btn_SignIn}"
                    TextColor="{AppThemeBinding Light={StaticResource Primary},
                                                Dark={StaticResource DarkPrimary}}"
                    TextDecorations="Underline">
                    <horus:MaterialLabel.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding SignInCommand}" />
                    </horus:MaterialLabel.GestureRecognizers>
                </horus:MaterialLabel>
            </HorizontalStackLayout>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>