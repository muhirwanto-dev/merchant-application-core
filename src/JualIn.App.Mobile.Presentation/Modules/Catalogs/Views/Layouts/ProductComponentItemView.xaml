<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    x:Class="JualIn.App.Mobile.Presentation.Modules.Catalogs.Views.Layouts.ProductComponentItemView"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:horus="clr-namespace:HorusStudio.Maui.MaterialDesignControls;assembly=HorusStudio.Maui.MaterialDesignControls"
    xmlns:icons="clr-namespace:UraniumUI.Icons.MaterialSymbols;assembly=UraniumUI.Icons.MaterialSymbols"
    xmlns:pages="clr-namespace:JualIn.App.Mobile.Presentation.Modules.Catalogs.Views"
    xmlns:strings="clr-namespace:JualIn.App.Mobile.Presentation.Resources.Strings"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    xmlns:uranium="http://schemas.enisn-projects.io/dotnet/maui/uraniumui/material"
    xmlns:validations="clr-namespace:InputKit.Shared.Validations;assembly=InputKit.Maui"
    xmlns:vm="clr-namespace:JualIn.App.Mobile.Presentation.Modules.Catalogs.ViewModels"
    x:DataType="{x:Type vm:ProductComponentViewModel}">
    <horus:MaterialCard
        Padding="{StaticResource CommonSpaceSmallAll}"
        BorderColor="{AppThemeBinding Light={StaticResource PrimaryContainer},
                                      Dark={StaticResource DarkPrimaryContainer}}"
        Command="{Binding Path=ViewModel.OpenInventoryDetailCommand, Source={RelativeSource AncestorType={x:Type pages:ProductFormPage}}, x:DataType={x:Type pages:ProductFormPage}}"
        CommandParameter="{Binding .}"
        CornerRadius="{StaticResource MaterialCornerRadiusMedium}"
        Type="Outlined">
        <Grid
            ColumnDefinitions="auto,*,auto"
            ColumnSpacing="{StaticResource CommonSpaceBetweenSmall}"
            RowDefinitions="auto,auto,auto,auto,auto,auto"
            VerticalOptions="Start">
            <Border
                Grid.Row="0"
                Grid.RowSpan="4"
                Grid.Column="0"
                BackgroundColor="{StaticResource SurfaceContainer}"
                HeightRequest="{StaticResource InventorySummaryImageSize}"
                StrokeShape="{RoundRectangle CornerRadius={StaticResource MaterialCornerRadiusMedium}}"
                StrokeThickness="0"
                WidthRequest="{StaticResource InventorySummaryImageSize}">
                <Image Aspect="AspectFill" Source="{Binding Inventory.Image, Converter={toolkit:ByteArrayToImageSourceConverter}}" />
            </Border>

            <horus:MaterialLabel
                Grid.Row="0"
                Grid.Column="1"
                FontAttributes="Bold"
                HorizontalOptions="Fill"
                Text="{Binding Inventory.Name}"
                Type="TitleSmall" />
            <horus:MaterialIconButton
                Grid.Row="0"
                Grid.RowSpan="2"
                Grid.Column="2"
                HorizontalOptions="End"
                ImageSource="{FontImageSource FontFamily=MaterialRounded,
                                              Glyph={Static icons:MaterialRounded.Chevron_right}}"
                VerticalOptions="Start" />
            <horus:MaterialLabel
                Grid.Row="1"
                Grid.Column="1"
                HorizontalOptions="Fill"
                Text="{Binding Inventory.InventoryId, StringFormat='{}ID: {0}'}"
                Type="LabelMedium" />

            <horus:MaterialLabel
                Grid.Row="3"
                Grid.Column="1"
                HorizontalOptions="Fill"
                Type="LabelSmall"
                VerticalOptions="End">
                <horus:MaterialLabel.FormattedText>
                    <FormattedString>
                        <Span Text="{Static strings:AppStrings.Inventory_Lbl_Stock}" />
                        <Span Text=":" />
                        <Span Text="{Binding Inventory.Stock, StringFormat='{} {0}'}" />
                        <Span Text="{Binding Inventory.StockUnit, StringFormat='{} {0}'}" />
                    </FormattedString>
                </horus:MaterialLabel.FormattedText>
            </horus:MaterialLabel>

            <uranium:TextField
                Title="{Static strings:AppStrings.ProductComponent_Lbl_QuantityPerUnit}"
                Grid.Row="4"
                Grid.Column="0"
                Grid.ColumnSpan="3"
                Margin="{StaticResource CommonSpaceSmallTop}"
                AllowClear="False"
                ReturnCommand="{Binding ViewModel.QuantityPerUnitChangedCommand, Source={RelativeSource AncestorType={x:Type pages:ProductFormPage}}, x:DataType={x:Type pages:ProductFormPage}}"
                Text="{Binding QuantityPerUnit}"
                TextChanged="QuantityPerUnit_TextChanged">
                <uranium:TextField.Validations>
                    <validations:NumericValidation />
                </uranium:TextField.Validations>
            </uranium:TextField>
        </Grid>
    </horus:MaterialCard>
</ContentView>
